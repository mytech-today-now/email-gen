# Addenda

Place optional addendum files in this directory. Supported formats are `.html`, `.txt`, and `.md`.

HTML addenda are sanitized for email-safe markup before rendering. Text and Markdown addenda are converted to simple paragraphs and safe links. Large files are rejected according to `MAX_ADDENDUM_BYTES`.
